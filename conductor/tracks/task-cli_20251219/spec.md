# spec.md

## Track: Implement Core Task Management Features

### Requirements
- Develop CLI interface with dynamic imports for fast startup
- Implement task storage using `conf` with local persistence
- Create React/Ink GUI with real-time task updates
- Add priority system (high/medium/low) with visual indicators
- Implement search/filter by text and @tags
- Ensure task sorting by priority and completion status
- Add custom pink-themed UI components
- Integrate date-fns for time calculations
- Use nanoid for unique task IDs
- Set up MCP server for task synchronization (optional)